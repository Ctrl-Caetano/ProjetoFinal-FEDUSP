version: 2

models:
  - name: fact_notifications
    columns:
      - name: notification_sk
        tests:
          - not_null
      
      - name: flag_obito
        tests:
          - accepted_values:
              values: [0, 1]

  - name: dim_patient
    columns:
      - name: patient_sk
        tests:
          - unique
          - not_null
